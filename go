# GO language stuff

# this is all stuff I was told to include when setting up bash_profile.
# if ever GO things aren't working probably something here needs tweaking
export GOPATH=$HOME/go/hail:$HOME/go:$HOME/bin
export GOFLAGS=""
export GOPROXY=https://proxy.golang.org
